define([], function () {
    var configLocal = {};

    configLocal.api = {
        name: 'OHDSI',
        url: 'http://{{ inventory_hostname }}:{{ www_port }}/WebAPI/'
    };

    return configLocal;
});